language: python
python:
  - "2.7"

# command to install dependencies
install:
  - pip install -r requirements.txt

# command to run tests
script:
  - cd testcase2owl; python -m pytest; cd ..
  - travis_wait 60 py.test ./tests
